spring:
  datasource:
    url: jdbc:h2:mem:telecomdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: telecom_admin
    # VULNERABILITY: Hardcoded database password in application config.
    password: TelecomDbPassword123!
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always

app:
  jwt:
    # VULNERABILITY: Hardcoded JWT secret in source-controlled config.
    secret: hardcoded-very-weak-demo-secret
  external:
    # VULNERABILITY: Hardcoded API key in plaintext.
    billing-api-key: hardcoded-billing-api-key-demo

